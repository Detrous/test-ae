

### Configuration
For simplicity use the default configuration and edit it if necessary:
configuration/defaults/service.yaml

### Run
```
docker-compose up
```

The application will be available at 127.0.0.1:8822